# 이 디렉터리를 Lombok 설정 루트로 고정(상위 디렉터리 탐색 중단)
config.stopBubbling = true

# Lombok 생성 메서드(@Getter/@Builder/@Data 등)에 @lombok.Generated 를 부여한다.
# → JaCoCo(0.8.2+)와 SonarQube가 생성 코드를 커버리지·분석에서 자동 제외하여,
#   테스트 대상이 아닌 보일러플레이트가 커버리지를 왜곡하지 않게 한다.
lombok.addLombokGeneratedAnnotation = true
